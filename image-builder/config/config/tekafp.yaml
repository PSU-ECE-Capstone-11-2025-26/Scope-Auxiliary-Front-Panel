include:
  file: trixie-minbase.yaml

device:
  layer: rpi4
  class: pi4
  hostname: tek-afp
  user1: tek
  user1pass: Team11Capstone!
  storage_type: sd

image:
  name: tek-afp-pi4
  compression: zstd
  rootfs_type: ext4

layer:
  splash: rpi-splash-screen
  app: afpprod

splash:
  image_path: ${@SRCROOT}/splash.tga

packages:
  - vim
  - packages/afp-ui_arm64.deb
