# REV A Firmware

## Files
- `RevAmain.py` — main Pico firmware for the REV A PCB

## Deployment
Rename `RevAmain.py` to `main.py` when copying it to the Pico.

## Tests
REV A test scripts are stored in `interface/tests/revA/`.
